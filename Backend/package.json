{
  "name": "backend",
  "version": "1.0.0",
  "description": "### /login (post) - returns error message if login fails  - returns user information upon successful login",
  "main": "index.js",
  "scripts": {
    "test": "npm run start-dev && jest",
    "start": "nodemon index.js",
    "start-dev": "knex migrate:rollback && knex migrate:latest && knex seed:run",
    "clear_jest": "jest --clearCache"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.0",
    "express": "^4.18.2",
    "jest": "^29.5.0",
    "knex": "^2.4.2",
    "nodemon": "^2.0.22",
    "pg": "^8.11.0",
    "supertest": "^6.3.3"
  }
}
